<Window x:Class="MVVMSettings.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:MVVMSettings.MVVM.ViewModels"
        xmlns:views="clr-namespace:MVVMSettings.MVVM.Views"
        
        
        mc:Ignorable="d"
        Height="600" Width="1000"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        
        >

    <Grid MaxHeight="600" MaxWidth="1000">

        <views:MainWindowView Loaded="MainWindowView_Loaded"/>

        <Grid VerticalAlignment="Top" Height="26" Background="Transparent" MouseLeftButtonDown="Grid_MouseLeftButtonDown">

            <!-- Minimize Button -->
            <Button 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Top" 
                    Width="40" 
                    Height="25" 
                    Click="MinimizeButton_Click" 
                    Margin="0,1,42,0"
                    Background="Transparent"
                    Foreground="White"
                    BorderThickness="0"
                    >
                <Button.Content>
                    <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="12" Text="" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Button.Content>

            </Button>

            <!-- Exit Button -->
            <Button
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Top" 
                    Margin="0,1,0,0"
                    Width="40" 
                    Height="25" 
                    Click="ExitButton_Click"
                    Background="Transparent"
                    Foreground="White"
                    BorderBrush="Transparent"
                    BorderThickness="0">
                <Button.Content>
                    <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="14" Text="" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Button.Content>
            </Button>


        </Grid>
        
        
       

    </Grid>
    
    
</Window>
